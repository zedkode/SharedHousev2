{
  "name": "@sharedhouse/workers",
  "version": "0.3.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@sharedhouse/contracts": "0.3.6",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/pg": "^8.21.0",
    "tsx": "^4.20.3",
    "vitest": "^4.1.10"
  }
}
