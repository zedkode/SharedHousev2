# Decision entries

Store durable decisions with context, chosen option, alternatives, consequences and review trigger. Prefer the project ADR mechanism if one exists; this folder may index/summarise it.
