# Shared UI components

The repository currently has no extracted React primitive library. The administration foundation is
implemented as one self-contained page in `apps/admin-web/src/App.tsx`; its cards, labels and status
rows are page-specific and are not reused by another route.

Mobile components listed in `docs/01-ux-ui/design-system.md` are specifications only and do not have
source implementations yet.
